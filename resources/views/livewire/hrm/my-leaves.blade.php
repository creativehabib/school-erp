<section class="w-full space-y-6"><div><flux:heading size="xl">{{ __('My Leaves') }}</flux:heading><flux:text>{{ __('Apply for leave and follow approval status.') }}</flux:text></div><flux:card><form wire:submit="submit" class="grid gap-4 md:grid-cols-2"><flux:select wire:model="leaveTypeId" :label="__('Leave type')"><flux:select.option value="">{{ __('Select type') }}</flux:select.option>@foreach($this->leaveTypes as $type)<flux:select.option wire:key="leave-type-{{ $type->id }}" :value="$type->id">{{ $type->name }}</flux:select.option>@endforeach</flux:select><div></div><flux:input wire:model="fromDate" type="date" :label="__('From date')"/><flux:input wire:model="toDate" type="date" :label="__('To date')"/><div class="md:col-span-2"><flux:textarea wire:model="reason" :label="__('Reason')"/></div><div class="md:col-span-2 flex justify-end"><flux:button type="submit" variant="primary">{{ __('Submit request') }}</flux:button></div></form></flux:card><div class="space-y-3">@foreach($this->applications as $application)<flux:card wire:key="my-leave-{{ $application->id }}" class="flex items-center justify-between gap-4"><div><div class="font-medium">{{ $application->leaveType?->name }} — {{ $application->from_date->format('d M') }} to {{ $application->to_date->format('d M Y') }}</div><flux:text>{{ $application->reason }}</flux:text></div><flux:badge :color="$application->status->color()">{{ $application->status->label() }}</flux:badge></flux:card>@endforeach</div></section>
